/**
 * PhotoMode.js — SCAFFOLDING, filled in at Milestone 8 (GDD §12.12, §25).
 * Non-interactive capture tool built on the existing VFX <canvas> layer:
 * hides UI, offers a vignette/filter adjustment, exports a PNG. Unlocked
 * after Chapter 1; the Main Menu shows it as a locked nav item until then.
 */
export class PhotoMode {
  constructor(eventBus) {
    this.eventBus = eventBus;
  }

  open() {
    console.warn('[PhotoMode] open() — not available until post-Chapter-1 content exists (Milestone 8+).');
  }
}
